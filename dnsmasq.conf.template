no-resolv

server=1.1.1.1
server=8.8.8.8

listen-address=127.0.0.1
listen-address=::1

listen-address={MAIN_IPV4}
listen-address={MAIN_IPV6}

# В entrypoint.sh будут добавляться строки address=/домен/IP
